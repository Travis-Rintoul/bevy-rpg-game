// use bevy::prelude::*;

// pub struct StateManagerPlugin;

// impl Plugin for StateManagerPlugin {
//     fn build(&self, app: &mut App) {
//         // Set initial state of the game
//         app.insert_resource(State::new(GameScene::Loading));

//         // Add the systems that will handle state transitions
//         app.add_systems(Update, manage_game_states);
//     }
// }